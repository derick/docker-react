language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t derickh/docker-react -f Dockerfile.dev .

  script:
    - docker run -e CI=true derickh/docker-react npm run test -- --coverage